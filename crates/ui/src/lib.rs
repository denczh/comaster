//! Reusable Slint UI components for Comaster.
//!
//! This crate houses shared `.slint` component definitions and their Rust
//! companion code: custom rendering helpers, data-binding adapters, and
//! any platform-specific UI utilities. Components here are consumed by the
//! main `comaster` application crate via `slint::include_modules!()`.
